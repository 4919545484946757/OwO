# v0.4 完成度与验收报告

> 🗄️ **已合并归档（2026-08-13）**：本文件结论已并入《技术文档-AI智能体输入法.md》v0.5 附录 C（完成度与验收基线）；逐项细节仍以 `agent-sdk/ACCEPTANCE.md` 为准。

> 日期：2026-08-12 ｜ 分支：agent-frame（157 个提交） ｜ 上游：`技术文档-AI智能体输入法.md`（v0.4，已合并 v0.4 续写计划）

## 1. 结论

v0.3 技术路线审计：**M1/M2 已完成，M3/M4 部分/未开始**（详见 `技术路线完成度审计-2026-08-12.md`）。
v0.4 续写计划的可实施项**已全部实现并通过本环境可执行的验证**；剩余为依赖外部数据/交互环境的验收口径项。

## 2. 逐里程碑完成情况

| 里程碑 | 计划内容 | 状态 | 主要证据 |
|---|---|---|---|
| M3 桌面端 + 技能包 | Tauri 桌面主客户端（任务/审批/diff/技能中心/感知状态/自动化/设置与诊断）；内置四技能包与契约门禁 | ✅ | `desktop/tauri`（Web 工作台 + Tauri 壳 + 托盘 + 全局快捷键 + 自启 + NSIS 安装包 + updater）；`skills/{documents,spreadsheets,pdf,browser}` 12 个端到端用例 `skill-gate.ps1` 全绿；`automation.rs` 定时任务/提醒/审计；数据出境开关（7.5）：`settings.egress` + 网关联网前拒绝 + `GET /settings` / `POST /settings/egress` + Web 一键切换；**P1 桌面端可实施项全部闭环**：会话管理（title/归档/置顶/会话树/fork/rewind/redo/断点恢复）、审计落库 + 日志面板、技能中心（启用/禁用/查看/编辑/删除/分享）、对话附件（上传/清洗/注入）、模型热切换（真实请求体验证）；唯一可选未做项：内置终端（可选，非验收必需） |
| M4 全域感知 + 语音 | L0 前台/剪贴板掩码、L1 无障碍 UI 树、L2 截图 + OCR、本地 STT（SenseVoice-Small）、语音入口 | ✅ + 验收数据 | `platform.rs`/`accessibility.rs`/`ocr.rs`/`stt.rs`；快照 19 节点 UI 树、8x8 采样与 OCR 冒烟；STT：TTS CER 0.00%、FunASR 真实人声 CER 13.64%（标点归一化）、缓存 3.33s→0.93s；**VSCode 语音改代码 20/20=100% PASS**；中英混说 3 样本 CER 均值 22.21%；Web 🎤 本地闭环 |
| M5 操作学习 + 主动建议 | 示范学习/受限探索、动作图执行、流程技能包（沉淀/校验/分享）、执行审批与审计、主动建议 | ✅ | `learn.rs`（录制→泛化→沉淀）、`executor.rs`（UIA 锚点 + SendInput + ValuePattern 回读验证 + 敏感熔断）、`share_skill.rs`（.owskill）、执行 confirm/high_risk_ack、`proactive.rs`、桌面闭环 UI；**Notepad 真实桌面端到端 2/2**；E2E 修复：模型网关代理、工具会话工作区解析、审批事件去重 |
| 数据出境开关（7.5） | 设置页云能力总开关；关闭后拒绝需要云模型的任务 | ✅ | `settings.rs` EgressSettings（默认开）+ `gateway.rs` complete/complete_stream 联网前拒绝（每次调用检查运行时开关）+ CLI serve/repl/tui/turn 启动应用 + Web“设置与诊断”区；切换写回 settings.json、即时生效并记入审计；E2E：关闭→turn 返回“云端模型已禁用”、运行中开启→恢复联网调用；契约测试 2 个 |
| 设置与诊断（P1） | 设置页可修改、改动即时生效并记入审计 | ✅ | `GET /settings` / `POST /settings`（保存完整 settings.json + 运行时应用数据出境/模型热切换/STT/主动建议/白名单 + 审计）；`whitelist/manage` 持久化用户清单；Web JSON 编辑器 + 保存按钮；E2E：POST /settings 后数据出境即时生效、白名单运行时生效、写回 settings.json；契约测试 4 个（settings 往返、STT apply_settings、ProactiveEngine apply_settings、网关模型热切换） |
| 会话管理（P1） | 侧边栏任务列表：新建/继续/fork/归档/置顶/重命名；会话树与断点恢复 | ✅ | session.rs title/archived/pinned + SQLite 列迁移；`GET /session/{id}` 历史断点恢复；`POST /session/{id}/rename|archive|pin`；列表置顶排序、归档默认隐藏；Web 会话树 + 继续/重命名/置顶/归档/fork/回退/重做；E2E 全通（含重启持久化）；契约测试 3 个 |
| 审计落库 + 日志面板（P1） | 日志/审计：可查询的持久化审计 | ✅ | SessionStore `append_audit` / `recent_audit`（SQLite，按条目 session_id）；服务端统一 flush（回合/设置/学习）；`GET /audit?limit=N`；Web 审计日志面板；E2E：egress 审计落库 + 重启可查 |
| 技能中心（P1） | 内置技能包 + 用户流程技能包列表：启用/禁用、查看/编辑/删除/分享 | ✅ | SkillRegistry 运行时禁用集合（注入/use_skill 过滤，即时生效）；`skills.disabled` 持久化；`GET/POST /skills/{name}`、`POST /skills/{name}/enabled`、`GET/DELETE /learn/packages/{name}`；Web 启用/禁用/查看/编辑/导出/删除；E2E 7 步全通；契约测试 2 个（禁用过滤+共享即时、settings 往返） |
| 对话附件（P1） | 对话与输入：附件（图片/文件） | ✅ | `POST/GET /session/{id}/attachments`（base64 JSON、文件名清洗、50MB 上限、工作区 `.owo-attachments/`）；`TurnRequest.attachments` 注入路径上下文；Web 📎 多选上传 + chips；E2E 7 步全通；契约测试 1 个（附件名清洗） |
| 桌面操作迭代（P3/computer-use） | 主动打开应用 + 模拟鼠标点击 + 深层 UI 定位 + OCR 截图验证 | ✅ 能力层 + QQ 三实锤 + 微信就绪 | `ActionType::Launch`/`ClickAt`；`inject` 修复；`POST /perception/tree`（含坐标）；`POST /perception/ocr`；OCR 根因修复；`SemanticAnchor.parent`；`ui:` 树断言；**QQ 文本/文件/表情发送实锤**（hello.txt 已接收、[呲牙] 已上屏）；微信已启动（登录窗口：二维码/扫码登录），待扫码登录后测消息/文件；红包面板图像化（需视觉/模板迭代，金额待确认）；注入式聊天内容被忽略 |
| M6 输入法融合 | TSF/IMK 壳 + librime 复用 | 占位不实施 | 前置条件评审文档已产出（`输入法融合-P4前置条件评审-2026-08-12.md`：2 满足/3 部分/1 未启动）；D5–D7 保持“不实施” |
| 文档 | 主文档升级 v0.4（D17–D26、5.8/6.5/7.6、M3–M6 路线图、术语） | ✅ | `技术文档-AI智能体输入法.md`（v0.4） |

## 3. 质量门禁现状（2026-08-12 实测）

| 门禁 | 结果 |
|---|---|
| `cargo fmt --all -- --check` | ✅ 干净 |
| `cargo clippy --workspace --all-targets -D warnings` | ✅ 0 警告（桌面壳单独 clippy 也干净） |
| `cargo test --workspace` | ✅ 106 个测试全绿（core 83 / cli 5 / eval 2 / loop 12 / mcp 3 / server 1） |
| `scripts/skill-gate.ps1` | ✅ 四技能 12 个端到端用例 PASS |
| HTTP 冒烟 | ✅ 会话/审批/diff/感知/学习/分享/STT/自动化/执行 全链路 |
| 打包 | ✅ 便携 zip（6.8MB）+ NSIS setup.exe（4.8MB，含核心服务）+ updater latest.json |

注：sherpa-onnx 静态库与 Rust 运行时存在 LNK4098（/MT vs /MD）链接告警，不影响运行，已记录。

## 4. 剩余外部验收项（需外部数据/交互环境）

| 项 | 验收口径（v0.4 计划） | 当前状态 | 需要什么 |
|---|---|---|---|
| STT 自然语音 WER 基线 | 50 中文 + 20 中英混说 WER<5%，5s 音频 p95<2s | 中文：TTS 0%、FunASR 真实人声 13.64%；混说 3 样本均值 22.21%；延迟达标（0.93s） | 带标注的普通话语料（评估工具 `stt-wer-eval.py` 就绪） |
| VSCode 语音改代码 | 20 次成功率 ≥80% | ✅ **20/20=100%**（`voice_code_batch.py`） | 已达成；可扩为不同任务类型复跑 |
| QQ 发文件流程复用 | 示范一次后换参数复用 ≥80% | 录制/泛化/沉淀/执行/回读验证均通过（Notepad 2/2） | QQ 登录会话与测试账号（S2 测试会话） |
| 自动观察（QQ 场景） | 聊天授权 + 消息掩码 + 发送预览审批 | 自动观察实机已验证（前台/剪贴板去重采样）；QQ 窗口可达、技能包已导入 | QQ 主窗口切前台 + 测试联系人/文件 |
| 桌面会话实机验证 | 本地面板唤起 p95 < 150ms；前台/剪贴板/截图在真实桌面会话中可用 | ✅ 核心链路实测：前台切换/剪贴板掩码/L2 截图/L3 假设全部工作；IPC p95 1.26ms | 仅剩 QQ 主窗口端到端发文件 |
| P4 输入法融合 | 前置条件评审 | 占位 | 决策层评审 |

## 5. 开放问题建议默认

- 聊天类白名单首批：QQ / 微信 / 飞书（已在 `whitelist.rs` 默认清单）；授权粒度为**单会话级**（`chat_authorized` 逐会话开启），消息内容默认掩码，发送动作预览 + 审批。
- `.owskill` 分享：权限仅允许白名单集合，敏感度必填，zip-slip 拒绝（已实现并测试）。
- 自动更新：静态 `latest.json` + 签名验证已就绪，托管到任意静态服务器并替换 `plugins.updater.endpoints` 即可启用。

## 6. 运行入口

```powershell
cd agent-sdk
cargo run -p owo-agent-cli -- serve --port 4096 --workspace .   # 核心服务（浏览器打开 http://127.0.0.1:4096/）
cargo run -p owo-agent-cli -- tui --workspace .                 # TUI
scripts\package-desktop.ps1 -Configuration release              # 便携包
scripts\build-installer.ps1                                     # NSIS 安装包
scripts\download-stt-model.ps1                                  # SenseVoice 模型
```
