你现在操作一个模拟 QQ 聊天窗口（虚拟桌面，窗口大小为 1020x700），左侧联系人列表里有“张子豪”和“李四”两个人。
请完成：
1. 用 screen_ocr 读取屏幕：左侧联系人行（role_hint=contact 或 active_contact）带坐标，
   每条联系人下面有最近消息预览（role_hint=preview），右侧显示当前会话内容。
2. 先切换到“李四”：点击左侧“李四”行中心（注意预览里显示“项目进度文档”），
   读取李四发来的消息，输入一句合适的回复并点击“发送”，再用 screen_ocr 验证已上屏且输入框清空。
3. 再切换到“张子豪”：点击左侧“张子豪”行中心，读取消息（今晚吃什么、想吃清淡），
   输入一句合适的回复并点击“发送”，再验证。
4. 用 desktop_wait_until 等待对方回复（例如等待包含“好呀”或“看了”的文本出现，timeout 15 秒），
   读取新回复后向当前联系人再补一句确认并发送。
最后报告：两个联系人各自的聊天内容、发送的每条消息、验证结果。
